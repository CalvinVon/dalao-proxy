const defaultOptions = {

};

function configureSetting() {
    return {
        defaultEnable: true,
        userOptionsField: '<%= pluginName %>',
        configureEnableField: 'enable',
    }
}

function parser(rawUserConfig) {
    return {

    };
}

module.exports = {
    configureSetting,
    parser
};