const defaultOptions = {

};

function setting() {
    return {
        defaultEnable: true,
        optionsField: '<%= pluginName %>',
        enableField: 'enable',
    }
}

function parser(rawUserConfig) {
    return {

    };
}

module.exports = {
    setting,
    parser
};