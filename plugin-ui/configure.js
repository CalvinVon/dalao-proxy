const defaultOptions = {

};

function setting() {
    return {
        defaultEnable: true,
        optionsField: 'plugin-ui',
        enableField: 'enable',
    }
}

function parser(rawUserConfig) {
    return {

    };
}

module.exports = {
    setting,
    parser
};