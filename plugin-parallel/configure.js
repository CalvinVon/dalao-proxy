const defaultOptions = {

};

function setting() {
    return {
        defaultEnable: true,
        optionsField: 'plugin-parallel',
        enableField: 'enable',
    }
}

function parser(rawUserConfig) {
    return {

    };
}

module.exports = {
    setting,
    parser
};
